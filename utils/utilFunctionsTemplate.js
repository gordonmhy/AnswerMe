// Define functions that do repetitive operations in the utils/ folder, just like this one.
// This makes overall code much cleaner and more maintainable.

const aUtilFunction = () => {
    // This is a function that does something
}